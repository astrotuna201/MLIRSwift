module CDialects [system] {
  header "CDialects.h"

  /**
   We currently link all dependencies in CMLIR (because I haven't written a good script to list the transitive dependencies of targets). Eventually, we hope to separate out the dependencies only required by CDialects
   */
  link "MLIRCAPIStandard"
}
